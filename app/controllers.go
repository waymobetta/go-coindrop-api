// Code generated by goagen v1.4.1, DO NOT EDIT.
//
// API "coindrop": Application Controllers
//
// Command:
// $ goagen
// --design=github.com/waymobetta/go-coindrop-api/design
// --out=$(GOPATH)/src/github.com/waymobetta/go-coindrop-api
// --version=v1.4.1

package app

import (
	"context"
	"github.com/goadesign/goa"
	"github.com/goadesign/goa/cors"
	"net/http"
)

// initService sets up the service encoders, decoders and mux.
func initService(service *goa.Service) {
	// Setup encoders and decoders
	service.Encoder.Register(goa.NewJSONEncoder, "application/json")
	service.Encoder.Register(goa.NewGobEncoder, "application/gob", "application/x-gob")
	service.Encoder.Register(goa.NewXMLEncoder, "application/xml")
	service.Decoder.Register(goa.NewJSONDecoder, "application/json")
	service.Decoder.Register(goa.NewGobDecoder, "application/gob", "application/x-gob")
	service.Decoder.Register(goa.NewXMLDecoder, "application/xml")

	// Setup default encoder and decoder
	service.Encoder.Register(goa.NewJSONEncoder, "*/*")
	service.Decoder.Register(goa.NewJSONDecoder, "*/*")
}

// HealthcheckController is the controller interface for the Healthcheck actions.
type HealthcheckController interface {
	goa.Muxer
	Show(*ShowHealthcheckContext) error
}

// MountHealthcheckController "mounts" a Healthcheck resource controller on the given service.
func MountHealthcheckController(service *goa.Service, ctrl HealthcheckController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/health", ctrl.MuxHandler("preflight", handleHealthcheckOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowHealthcheckContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleHealthcheckOrigin(h)
	service.Mux.Handle("GET", "/v1/health", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Healthcheck", "action", "Show", "route", "GET /v1/health")
}

// handleHealthcheckOrigin applies the CORS response headers corresponding to the origin.
func handleHealthcheckOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// ProfilesController is the controller interface for the Profiles actions.
type ProfilesController interface {
	goa.Muxer
	Create(*CreateProfilesContext) error
	List(*ListProfilesContext) error
	Show(*ShowProfilesContext) error
	Update(*UpdateProfilesContext) error
}

// MountProfilesController "mounts" a Profiles resource controller on the given service.
func MountProfilesController(service *goa.Service, ctrl ProfilesController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/profiles", ctrl.MuxHandler("preflight", handleProfilesOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/profiles/:userId", ctrl.MuxHandler("preflight", handleProfilesOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewCreateProfilesContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*ProfilePayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Create(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleProfilesOrigin(h)
	service.Mux.Handle("POST", "/v1/profiles", ctrl.MuxHandler("create", h, unmarshalCreateProfilesPayload))
	service.LogInfo("mount", "ctrl", "Profiles", "action", "Create", "route", "POST /v1/profiles", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewListProfilesContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.List(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleProfilesOrigin(h)
	service.Mux.Handle("GET", "/v1/profiles", ctrl.MuxHandler("list", h, nil))
	service.LogInfo("mount", "ctrl", "Profiles", "action", "List", "route", "GET /v1/profiles", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowProfilesContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleProfilesOrigin(h)
	service.Mux.Handle("GET", "/v1/profiles/:userId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Profiles", "action", "Show", "route", "GET /v1/profiles/:userId", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateProfilesContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*ProfilePayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleProfilesOrigin(h)
	service.Mux.Handle("POST", "/v1/profiles/:userId", ctrl.MuxHandler("update", h, unmarshalUpdateProfilesPayload))
	service.LogInfo("mount", "ctrl", "Profiles", "action", "Update", "route", "POST /v1/profiles/:userId", "security", "JWTAuth")
}

// handleProfilesOrigin applies the CORS response headers corresponding to the origin.
func handleProfilesOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalCreateProfilesPayload unmarshals the request body into the context request data Payload field.
func unmarshalCreateProfilesPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &profilePayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// unmarshalUpdateProfilesPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateProfilesPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &profilePayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// QuizzesController is the controller interface for the Quizzes actions.
type QuizzesController interface {
	goa.Muxer
	Create(*CreateQuizzesContext) error
	List(*ListQuizzesContext) error
	Show(*ShowQuizzesContext) error
}

// MountQuizzesController "mounts" a Quizzes resource controller on the given service.
func MountQuizzesController(service *goa.Service, ctrl QuizzesController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/quizzes", ctrl.MuxHandler("preflight", handleQuizzesOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/quizzes/:quizId", ctrl.MuxHandler("preflight", handleQuizzesOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewCreateQuizzesContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*QuizPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Create(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleQuizzesOrigin(h)
	service.Mux.Handle("POST", "/v1/quizzes", ctrl.MuxHandler("create", h, unmarshalCreateQuizzesPayload))
	service.LogInfo("mount", "ctrl", "Quizzes", "action", "Create", "route", "POST /v1/quizzes", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewListQuizzesContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.List(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleQuizzesOrigin(h)
	service.Mux.Handle("GET", "/v1/quizzes", ctrl.MuxHandler("list", h, nil))
	service.LogInfo("mount", "ctrl", "Quizzes", "action", "List", "route", "GET /v1/quizzes", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowQuizzesContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleQuizzesOrigin(h)
	service.Mux.Handle("GET", "/v1/quizzes/:quizId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Quizzes", "action", "Show", "route", "GET /v1/quizzes/:quizId", "security", "JWTAuth")
}

// handleQuizzesOrigin applies the CORS response headers corresponding to the origin.
func handleQuizzesOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalCreateQuizzesPayload unmarshals the request body into the context request data Payload field.
func unmarshalCreateQuizzesPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &quizPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// RedditController is the controller interface for the Reddit actions.
type RedditController interface {
	goa.Muxer
	Display(*DisplayRedditContext) error
	Show(*ShowRedditContext) error
	Update(*UpdateRedditContext) error
	Verify(*VerifyRedditContext) error
}

// MountRedditController "mounts" a Reddit resource controller on the given service.
func MountRedditController(service *goa.Service, ctrl RedditController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/social/reddit/:userId/verify", ctrl.MuxHandler("preflight", handleRedditOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/social/reddit/:userId", ctrl.MuxHandler("preflight", handleRedditOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/social/reddit", ctrl.MuxHandler("preflight", handleRedditOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewDisplayRedditContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Display(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleRedditOrigin(h)
	service.Mux.Handle("GET", "/v1/social/reddit/:userId/verify", ctrl.MuxHandler("display", h, nil))
	service.LogInfo("mount", "ctrl", "Reddit", "action", "Display", "route", "GET /v1/social/reddit/:userId/verify", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowRedditContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleRedditOrigin(h)
	service.Mux.Handle("GET", "/v1/social/reddit/:userId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Reddit", "action", "Show", "route", "GET /v1/social/reddit/:userId", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateRedditContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*CreateUserPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleRedditOrigin(h)
	service.Mux.Handle("POST", "/v1/social/reddit", ctrl.MuxHandler("update", h, unmarshalUpdateRedditPayload))
	service.LogInfo("mount", "ctrl", "Reddit", "action", "Update", "route", "POST /v1/social/reddit", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewVerifyRedditContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*VerificationPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Verify(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleRedditOrigin(h)
	service.Mux.Handle("POST", "/v1/social/reddit/:userId/verify", ctrl.MuxHandler("verify", h, unmarshalVerifyRedditPayload))
	service.LogInfo("mount", "ctrl", "Reddit", "action", "Verify", "route", "POST /v1/social/reddit/:userId/verify", "security", "JWTAuth")
}

// handleRedditOrigin applies the CORS response headers corresponding to the origin.
func handleRedditOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalUpdateRedditPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateRedditPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &createUserPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// unmarshalVerifyRedditPayload unmarshals the request body into the context request data Payload field.
func unmarshalVerifyRedditPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &verificationPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// RedditharvestController is the controller interface for the Redditharvest actions.
type RedditharvestController interface {
	goa.Muxer
	Update(*UpdateRedditharvestContext) error
}

// MountRedditharvestController "mounts" a Redditharvest resource controller on the given service.
func MountRedditharvestController(service *goa.Service, ctrl RedditharvestController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/social/reddit/harvest", ctrl.MuxHandler("preflight", handleRedditharvestOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateRedditharvestContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*UpdateUserPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleRedditharvestOrigin(h)
	service.Mux.Handle("POST", "/v1/social/reddit/harvest", ctrl.MuxHandler("update", h, unmarshalUpdateRedditharvestPayload))
	service.LogInfo("mount", "ctrl", "Redditharvest", "action", "Update", "route", "POST /v1/social/reddit/harvest", "security", "JWTAuth")
}

// handleRedditharvestOrigin applies the CORS response headers corresponding to the origin.
func handleRedditharvestOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalUpdateRedditharvestPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateRedditharvestPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &updateUserPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// ResultsController is the controller interface for the Results actions.
type ResultsController interface {
	goa.Muxer
	Create(*CreateResultsContext) error
	List(*ListResultsContext) error
	Show(*ShowResultsContext) error
}

// MountResultsController "mounts" a Results resource controller on the given service.
func MountResultsController(service *goa.Service, ctrl ResultsController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/quizzes/results", ctrl.MuxHandler("preflight", handleResultsOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/quizzes/:quizId/results", ctrl.MuxHandler("preflight", handleResultsOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewCreateResultsContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*QuizResultsPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Create(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleResultsOrigin(h)
	service.Mux.Handle("POST", "/v1/quizzes/results", ctrl.MuxHandler("create", h, unmarshalCreateResultsPayload))
	service.LogInfo("mount", "ctrl", "Results", "action", "Create", "route", "POST /v1/quizzes/results", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewListResultsContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.List(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleResultsOrigin(h)
	service.Mux.Handle("GET", "/v1/quizzes/results", ctrl.MuxHandler("list", h, nil))
	service.LogInfo("mount", "ctrl", "Results", "action", "List", "route", "GET /v1/quizzes/results", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowResultsContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleResultsOrigin(h)
	service.Mux.Handle("GET", "/v1/quizzes/:quizId/results", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Results", "action", "Show", "route", "GET /v1/quizzes/:quizId/results", "security", "JWTAuth")
}

// handleResultsOrigin applies the CORS response headers corresponding to the origin.
func handleResultsOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalCreateResultsPayload unmarshals the request body into the context request data Payload field.
func unmarshalCreateResultsPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &quizResultsPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// StackoverflowController is the controller interface for the Stackoverflow actions.
type StackoverflowController interface {
	goa.Muxer
	Display(*DisplayStackoverflowContext) error
	Show(*ShowStackoverflowContext) error
	Update(*UpdateStackoverflowContext) error
	Verify(*VerifyStackoverflowContext) error
}

// MountStackoverflowController "mounts" a Stackoverflow resource controller on the given service.
func MountStackoverflowController(service *goa.Service, ctrl StackoverflowController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/social/stackoverflow/:userId/verify", ctrl.MuxHandler("preflight", handleStackoverflowOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/social/stackoverflow/:userId", ctrl.MuxHandler("preflight", handleStackoverflowOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/social/stackoverflow", ctrl.MuxHandler("preflight", handleStackoverflowOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewDisplayStackoverflowContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Display(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleStackoverflowOrigin(h)
	service.Mux.Handle("GET", "/v1/social/stackoverflow/:userId/verify", ctrl.MuxHandler("display", h, nil))
	service.LogInfo("mount", "ctrl", "Stackoverflow", "action", "Display", "route", "GET /v1/social/stackoverflow/:userId/verify", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowStackoverflowContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleStackoverflowOrigin(h)
	service.Mux.Handle("GET", "/v1/social/stackoverflow/:userId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Stackoverflow", "action", "Show", "route", "GET /v1/social/stackoverflow/:userId", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateStackoverflowContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*CreateStackOverflowUserPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleStackoverflowOrigin(h)
	service.Mux.Handle("POST", "/v1/social/stackoverflow", ctrl.MuxHandler("update", h, unmarshalUpdateStackoverflowPayload))
	service.LogInfo("mount", "ctrl", "Stackoverflow", "action", "Update", "route", "POST /v1/social/stackoverflow", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewVerifyStackoverflowContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*VerificationPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Verify(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleStackoverflowOrigin(h)
	service.Mux.Handle("POST", "/v1/social/stackoverflow/:userId/verify", ctrl.MuxHandler("verify", h, unmarshalVerifyStackoverflowPayload))
	service.LogInfo("mount", "ctrl", "Stackoverflow", "action", "Verify", "route", "POST /v1/social/stackoverflow/:userId/verify", "security", "JWTAuth")
}

// handleStackoverflowOrigin applies the CORS response headers corresponding to the origin.
func handleStackoverflowOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalUpdateStackoverflowPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateStackoverflowPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &createStackOverflowUserPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// unmarshalVerifyStackoverflowPayload unmarshals the request body into the context request data Payload field.
func unmarshalVerifyStackoverflowPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &verificationPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// StackoverflowharvestController is the controller interface for the Stackoverflowharvest actions.
type StackoverflowharvestController interface {
	goa.Muxer
	Update(*UpdateStackoverflowharvestContext) error
}

// MountStackoverflowharvestController "mounts" a Stackoverflowharvest resource controller on the given service.
func MountStackoverflowharvestController(service *goa.Service, ctrl StackoverflowharvestController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/social/stackoverflow/harvest", ctrl.MuxHandler("preflight", handleStackoverflowharvestOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateStackoverflowharvestContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*UpdateStackOverflowUserPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleStackoverflowharvestOrigin(h)
	service.Mux.Handle("POST", "/v1/social/stackoverflow/harvest", ctrl.MuxHandler("update", h, unmarshalUpdateStackoverflowharvestPayload))
	service.LogInfo("mount", "ctrl", "Stackoverflowharvest", "action", "Update", "route", "POST /v1/social/stackoverflow/harvest", "security", "JWTAuth")
}

// handleStackoverflowharvestOrigin applies the CORS response headers corresponding to the origin.
func handleStackoverflowharvestOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalUpdateStackoverflowharvestPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateStackoverflowharvestPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &updateStackOverflowUserPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// TasksController is the controller interface for the Tasks actions.
type TasksController interface {
	goa.Muxer
	Create(*CreateTasksContext) error
	List(*ListTasksContext) error
	Show(*ShowTasksContext) error
	Update(*UpdateTasksContext) error
}

// MountTasksController "mounts" a Tasks resource controller on the given service.
func MountTasksController(service *goa.Service, ctrl TasksController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/tasks", ctrl.MuxHandler("preflight", handleTasksOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/tasks/:taskId", ctrl.MuxHandler("preflight", handleTasksOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewCreateTasksContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*CreateTaskPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Create(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleTasksOrigin(h)
	service.Mux.Handle("POST", "/v1/tasks", ctrl.MuxHandler("create", h, unmarshalCreateTasksPayload))
	service.LogInfo("mount", "ctrl", "Tasks", "action", "Create", "route", "POST /v1/tasks", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewListTasksContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.List(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleTasksOrigin(h)
	service.Mux.Handle("GET", "/v1/tasks", ctrl.MuxHandler("list", h, nil))
	service.LogInfo("mount", "ctrl", "Tasks", "action", "List", "route", "GET /v1/tasks", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowTasksContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleTasksOrigin(h)
	service.Mux.Handle("GET", "/v1/tasks/:taskId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Tasks", "action", "Show", "route", "GET /v1/tasks/:taskId", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateTasksContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*TaskPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleTasksOrigin(h)
	service.Mux.Handle("POST", "/v1/tasks/:taskId", ctrl.MuxHandler("update", h, unmarshalUpdateTasksPayload))
	service.LogInfo("mount", "ctrl", "Tasks", "action", "Update", "route", "POST /v1/tasks/:taskId", "security", "JWTAuth")
}

// handleTasksOrigin applies the CORS response headers corresponding to the origin.
func handleTasksOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalCreateTasksPayload unmarshals the request body into the context request data Payload field.
func unmarshalCreateTasksPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &createTaskPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// unmarshalUpdateTasksPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateTasksPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &taskPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// UsersController is the controller interface for the Users actions.
type UsersController interface {
	goa.Muxer
	Create(*CreateUsersContext) error
	List(*ListUsersContext) error
	Show(*ShowUsersContext) error
}

// MountUsersController "mounts" a Users resource controller on the given service.
func MountUsersController(service *goa.Service, ctrl UsersController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/users", ctrl.MuxHandler("preflight", handleUsersOrigin(cors.HandlePreflight()), nil))
	service.Mux.Handle("OPTIONS", "/v1/users/:userId", ctrl.MuxHandler("preflight", handleUsersOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewCreateUsersContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*UserPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Create(rctx)
	}
	h = handleUsersOrigin(h)
	service.Mux.Handle("POST", "/v1/users", ctrl.MuxHandler("create", h, unmarshalCreateUsersPayload))
	service.LogInfo("mount", "ctrl", "Users", "action", "Create", "route", "POST /v1/users")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewListUsersContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.List(rctx)
	}
	h = handleUsersOrigin(h)
	service.Mux.Handle("GET", "/v1/users", ctrl.MuxHandler("list", h, nil))
	service.LogInfo("mount", "ctrl", "Users", "action", "List", "route", "GET /v1/users")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowUsersContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleUsersOrigin(h)
	service.Mux.Handle("GET", "/v1/users/:userId", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Users", "action", "Show", "route", "GET /v1/users/:userId")
}

// handleUsersOrigin applies the CORS response headers corresponding to the origin.
func handleUsersOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalCreateUsersPayload unmarshals the request body into the context request data Payload field.
func unmarshalCreateUsersPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &userPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// WalletsController is the controller interface for the Wallets actions.
type WalletsController interface {
	goa.Muxer
	Show(*ShowWalletsContext) error
	Update(*UpdateWalletsContext) error
}

// MountWalletsController "mounts" a Wallets resource controller on the given service.
func MountWalletsController(service *goa.Service, ctrl WalletsController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/wallets", ctrl.MuxHandler("preflight", handleWalletsOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewShowWalletsContext(ctx, req, service)
		if err != nil {
			return err
		}
		return ctrl.Show(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleWalletsOrigin(h)
	service.Mux.Handle("GET", "/v1/wallets", ctrl.MuxHandler("show", h, nil))
	service.LogInfo("mount", "ctrl", "Wallets", "action", "Show", "route", "GET /v1/wallets", "security", "JWTAuth")

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewUpdateWalletsContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*WalletPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Update(rctx)
	}
	h = handleSecurity("JWTAuth", h)
	h = handleWalletsOrigin(h)
	service.Mux.Handle("POST", "/v1/wallets", ctrl.MuxHandler("update", h, unmarshalUpdateWalletsPayload))
	service.LogInfo("mount", "ctrl", "Wallets", "action", "Update", "route", "POST /v1/wallets", "security", "JWTAuth")
}

// handleWalletsOrigin applies the CORS response headers corresponding to the origin.
func handleWalletsOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalUpdateWalletsPayload unmarshals the request body into the context request data Payload field.
func unmarshalUpdateWalletsPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &walletPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	if err := payload.Validate(); err != nil {
		// Initialize payload with private data structure so it can be logged
		goa.ContextRequest(ctx).Payload = payload
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}

// WebhooksController is the controller interface for the Webhooks actions.
type WebhooksController interface {
	goa.Muxer
	Typeform(*TypeformWebhooksContext) error
}

// MountWebhooksController "mounts" a Webhooks resource controller on the given service.
func MountWebhooksController(service *goa.Service, ctrl WebhooksController) {
	initService(service)
	var h goa.Handler
	service.Mux.Handle("OPTIONS", "/v1/webhooks/typeform", ctrl.MuxHandler("preflight", handleWebhooksOrigin(cors.HandlePreflight()), nil))

	h = func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		// Check if there was an error loading the request
		if err := goa.ContextError(ctx); err != nil {
			return err
		}
		// Build the context
		rctx, err := NewTypeformWebhooksContext(ctx, req, service)
		if err != nil {
			return err
		}
		// Build the payload
		if rawPayload := goa.ContextRequest(ctx).Payload; rawPayload != nil {
			rctx.Payload = rawPayload.(*TypeformPayload)
		} else {
			return goa.MissingPayloadError()
		}
		return ctrl.Typeform(rctx)
	}
	h = handleWebhooksOrigin(h)
	service.Mux.Handle("POST", "/v1/webhooks/typeform", ctrl.MuxHandler("typeform", h, unmarshalTypeformWebhooksPayload))
	service.LogInfo("mount", "ctrl", "Webhooks", "action", "Typeform", "route", "POST /v1/webhooks/typeform")
}

// handleWebhooksOrigin applies the CORS response headers corresponding to the origin.
func handleWebhooksOrigin(h goa.Handler) goa.Handler {

	return func(ctx context.Context, rw http.ResponseWriter, req *http.Request) error {
		origin := req.Header.Get("Origin")
		if origin == "" {
			// Not a CORS request
			return h(ctx, rw, req)
		}
		if cors.MatchOrigin(origin, "*") {
			ctx = goa.WithLogContext(ctx, "origin", origin)
			rw.Header().Set("Access-Control-Allow-Origin", origin)
			rw.Header().Set("Access-Control-Allow-Credentials", "true")
			if acrm := req.Header.Get("Access-Control-Request-Method"); acrm != "" {
				// We are handling a preflight request
				rw.Header().Set("Access-Control-Allow-Methods", "OPTIONS, HEAD, POST, GET, UPDATE, DELETE, PATCH")
			}
			return h(ctx, rw, req)
		}

		return h(ctx, rw, req)
	}
}

// unmarshalTypeformWebhooksPayload unmarshals the request body into the context request data Payload field.
func unmarshalTypeformWebhooksPayload(ctx context.Context, service *goa.Service, req *http.Request) error {
	payload := &typeformPayload{}
	if err := service.DecodeRequest(req, payload); err != nil {
		return err
	}
	goa.ContextRequest(ctx).Payload = payload.Publicize()
	return nil
}
